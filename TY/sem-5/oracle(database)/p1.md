# Practical: stud_resultj

## Create Table

``` sql
CREATE TABLE stud_resultj (
    rno NUMBER(5),
    sname VARCHAR2(20),
    bdate DATE
);
```

## 1. Insert Records

``` sql
INSERT INTO stud_resultj VALUES (&rno,'&sname','&bdate');
SELECT * FROM stud_resultj;
```

    RNO SNAME   BDATE
  ----- ------- -----------
    101 joyeb   29-JAN-06
    102 mehul   04-FEB-07
    103 tusu    01-MAY-07
    104 jenik   05-APR-05
    105 sejan   25-JUN-09
    106 ziyan   05-DEC-04
    107 jacob   05-APR-02
    108 josef   20-JAN-06
    109 jay     06-FEB-05
    110 tapu    05-OCT-12

## 2. Add Columns

``` sql
ALTER TABLE stud_resultj
ADD (
  total NUMBER(5),
  per NUMBER(5,2),
  grade VARCHAR2(3)
);
```

## 3. Update Total

``` sql
UPDATE stud_resultj SET total=400 WHERE rno=101;
UPDATE stud_resultj SET total=300 WHERE rno=102;
UPDATE stud_resultj SET total=399 WHERE rno=103;
UPDATE stud_resultj SET total=299 WHERE rno=104;
UPDATE stud_resultj SET total=300 WHERE rno=105;
UPDATE stud_resultj SET total=250 WHERE rno=106;
UPDATE stud_resultj SET total=350 WHERE rno=107;
UPDATE stud_resultj SET total=150 WHERE rno=108;
UPDATE stud_resultj SET total=150 WHERE rno=109;
UPDATE stud_resultj SET total=380 WHERE rno=110;
```

``` sql
UPDATE stud_resultj SET per=total/4;
```

``` sql
UPDATE stud_resultj SET grade='A+' WHERE per>=95;
UPDATE stud_resultj SET grade='A' WHERE per<95 AND per>=90;
UPDATE stud_resultj SET grade='B' WHERE per<90 AND per>=75;
UPDATE stud_resultj SET grade='C' WHERE per<75 AND per>=55;
UPDATE stud_resultj SET grade='D' WHERE per<55 AND per>=45;
UPDATE stud_resultj SET grade='E' WHERE per<45 AND per>=35;
UPDATE stud_resultj SET grade='E' WHERE per<35;
```

## 4. Descending Order

``` sql
SELECT * FROM stud_resultj ORDER BY rno DESC;
```

## 5. Skip

## 6. Student Name and Birth Date

``` sql
SELECT sname,bdate FROM stud_resultj;
```

## 7. Total Between 150 and 190

``` sql
SELECT * FROM stud_resultj
WHERE total BETWEEN 150 AND 190
AND per<60;
```

## 8. Grade A or A+

``` sql
SELECT rno,sname,total
FROM stud_resultj
WHERE grade='A' OR grade='A+';
```

## 9. Delete Grade A or B

``` sql
DELETE FROM stud_resultj
WHERE grade='A' OR grade='B';
```

## 10. Delete Names Starting with S

``` sql
DELETE FROM stud_resultj
WHERE sname LIKE 'S%';
```

## 11. Delete Percentage Less Than 40

``` sql
DELETE FROM stud_resultj
WHERE per<40;
```
