````
# Assignment 15: Student Result

## Create Table

```sql
CREATE TABLE Stud_Result (
    Stud_rollno NUMBER(3),
    Stud_name VARCHAR2(15),
    City VARCHAR2(20),
    Oracle NUMBER(3),
    SE NUMBER(3),
    ASP NUMBER(3),
    Network NUMBER(3)
);
````

 ## 1\. Insert 10 Records

```
INSERT INTO Stud_Result VALUES (1,'Rahul','Mumbai',70,65,80,75);
INSERT INTO Stud_Result VALUES (2,'Priya','Botad',55,60,50,45);
INSERT INTO Stud_Result VALUES (3,'Amit','Rajkot',60,70,65,55);
INSERT INTO Stud_Result VALUES (4,'Sneha','Ahmedabad',45,50,52,48);
INSERT INTO Stud_Result VALUES (5,'Karan','Vadodara',80,85,75,90);
INSERT INTO Stud_Result VALUES (6,'Neha','Rajkot',52,58,54,50);
INSERT INTO Stud_Result VALUES (7,'Riya','Surat',40,42,45,50);
INSERT INTO Stud_Result VALUES (8,'Arjun','Botad',65,55,60,70);
INSERT INTO Stud_Result VALUES (9,'Mehul','Pune',75,72,68,80);
INSERT INTO Stud_Result VALUES (10,'Pooja','Delhi',90,88,85,92);
```

 ## 2\. Display All Students

```
SELECT * FROM Stud_Result;
```

 ## 3\. Display Table Structure

```
DESC Stud_Result;
```

 ## 4\. Add TOTAL and PER Columns

```
ALTER TABLE Stud_Result
ADD (TOTAL NUMBER(3), PER NUMBER(6,2));
```

 ## 5\. Update TOTAL and PER

```
UPDATE Stud_Result
SET TOTAL = Oracle + SE + ASP + Network;

UPDATE Stud_Result
SET PER = TOTAL / 4;
```

 ## 6\. Display Unique Cities

```
SELECT DISTINCT City FROM Stud_Result;
```

 ## 7\. Student Names in Descending Order

```
SELECT Stud_name
FROM Stud_Result
ORDER BY Stud_name DESC;
```

 ## 8\. Students from Botad and Rajkot

```
SELECT *
FROM Stud_Result
WHERE City IN ('Botad','Rajkot');
```

 ## 9\. Total Between 175 and 230

```
SELECT *
FROM Stud_Result
WHERE TOTAL BETWEEN 175 AND 230;
```

 ## 10\. Students Having 65 Marks in SE

```
SELECT *
FROM Stud_Result
WHERE SE = 65;
```

 ## 11\. Oracle Between 50–60 and PER \< 55

```
SELECT *
FROM Stud_Result
WHERE Oracle BETWEEN 50 AND 60
AND PER < 55;
```

 ## 12\. All Subjects Greater Than 40

```
SELECT *
FROM Stud_Result
WHERE Oracle > 40
AND SE > 40
AND ASP > 40
AND Network > 40;
```

 ## 13\. Students with Distinction

```
SELECT *
FROM Stud_Result
WHERE PER >= 70;
```

 ## 14\. Delete ASP \> 55 and Network \< 60

```
DELETE FROM Stud_Result
WHERE ASP > 55
AND Network < 60;
```

 ## 15\. Rename Table

```
RENAME Stud_Result TO student_result;
```

 ## Commit

```
COMMIT;
```

```

```
